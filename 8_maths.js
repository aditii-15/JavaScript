//? Constants:

const piValue = Math.PI;
console.log(piValue);

// todo Math.trunc and Math.sign
const truncValue = Math.trunc(-3.7); // Output - (-3)
const floorValue = Math.floor(-3.7); // Output - (-4)
console.log(truncValue);
console.log(floorValue);

//? Exponentional and Logarithm

// Math.pow(x, y) : Return the value of x to the power y
console.log(Math.pow(3, 4));
console.log(3 ** 4);